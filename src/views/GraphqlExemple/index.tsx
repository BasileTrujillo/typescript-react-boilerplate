import React from 'react';
import { AppLayout } from '../../layouts/AppLayout/AppLayout';
import GraphqlExemple from "./GraphqlExemple";

const GraphqlExempleRoute = (props: any) => {
  return (
    <AppLayout>
      <GraphqlExemple/>
    </AppLayout>
  );
};

export default GraphqlExempleRoute;
